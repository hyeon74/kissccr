// store/quizz.js
/*
import {
    db,
  } from '~/plugins/firebase'
  
  import { normalizeArrays } from '~/utils'
  import map from 'lodash/map'
  import { firebaseAction } from 'vuexfire'
  
  export const state = () => ({
    _quizzes: [],
  })
  
  export const mutations = {
  }
  
  export const getters = {
    quizzes ({ _quizzes }) {
      return map(_quizzes, normalizeArrays)
    },
  }
  
  export const actions = {
    bindQuizzes: firebaseAction(({ bindFirebaseRef }) => {
      bindFirebaseRef('_quizzes', db.ref('quizzes'))
    }),
  }
  */